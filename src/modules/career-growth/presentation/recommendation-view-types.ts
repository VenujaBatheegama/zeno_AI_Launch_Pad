import type {
  GrowthAssessment,
  GrowthMessage,
  GrowthRecommendation,
} from "../domain/schemas";

export type JobSearchCampaignLike = {
  id: string;
  name: string;
};

export type { GrowthAssessment, GrowthMessage, GrowthRecommendation };
