/**
 * Phase 1 defers unit-test updates. Keep a compile-safe placeholder.
 * Sample PDF inspection: `npx pnpm exec tsx scripts/generate-sample-cvs.ts`
 */
import { describe, expect, it } from "vitest";

describe("cv tailoring application (phase 1)", () => {
  it("placeholder — sample generation script covers render verification", () => {
    expect(true).toBe(true);
  });
});
